fastapi==0.95.2
uvicorn==0.22.0

# Core ML stack
torch
numpy

# HuggingFace stack (compatible versions)
transformers==4.36.2
sentence-transformers==2.2.2

# 🔧 Keras compatibility fix (REQUIRED)
tf-keras

# Optional but safe: prevent accidental Keras 3 install
keras<3